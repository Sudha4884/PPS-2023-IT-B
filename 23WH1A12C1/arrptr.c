#include<stdio.h>
void main()
{ 
int  a[10],*ptr,n,i;
ptr=a;
 printf("enter n value: ");
 scanf("%d", &n);
 printf("\n enter array elements: ");
 for(i=0;i<n;i++) 
 {
  scanf("%d",&a[i]);
 }
 ptr=a;
  for(i=0;i<n;i++)
   printf("%d",a[i]);
} 































































































































































































































































































































































































































































































































gcc++);

}
