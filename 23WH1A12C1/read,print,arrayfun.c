#include<stdio.h>
printArray(int x[],int n)
void printArray(int x[],int n)
void main()
{
 int A[100],n,i;
 n=readArray(A);
 printArray(A,n);
}
void main()
{
  scanf("%d",&n);
  for(i=0;i<n;i++)
    scanf("%d",&A[i]);
    printArray(A,n);
}
int readAray(int x[],int n)
{
  printf("enter n value: ");
  scanf("%d",&n);
  for(i=0;i<n;i++)
   scanf("%d",&x[i]);
}
void printArray(int num[],int n)
{
  int i,num;
  for(i=0;i<n;i++)
  {
    printf("%d",num[i]);
  }
}

