#print all prime numbers less than a given number

def is_prime(num):
  if num <= 1:
    return False
  for i in range(2, int(num**0.5) + 1):
    if num % i == 0:
      return False
  return True
def print_primes(n):
  for i in range(n):
    if is_prime(i):
      print(i)

print_primes(10)
