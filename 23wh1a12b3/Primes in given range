//printing primes in given ranges
#include<stdio.h>
int main()
{
  int i,num,n;
  printf("\n no.");
  scanf("%d",&n);
  for(i=1;i<=n;i++)
  {
    for(num=2;num<=i;num++)
    {
    if(i%num==0)
    break;
    }
    if(i==num)
    printf("\n%d",i);
   } 
}
