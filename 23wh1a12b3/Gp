//Geometric progression
#include<stdio.h>
#include<math.h>
int main()
{
  int x,n,i,sum=0;
  printf("\n enter x and n");
  scanf(" %d %d",&x,&n);
  printf("\n %d %d",x,n);
  for(i=0;i<=n;i++)
  {
    sum = sum+pow(x,i);
   
  }
  printf("\n The progression is %d",sum);
}
