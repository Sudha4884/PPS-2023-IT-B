#include <stdio.h>

int main(){
int a=55,b=65,c=50;

  
  if(a>b){
    if(a>c)
    printf("a is biggest of all three",a);
    else
    printf("c is bigger than a and b",c);
  }
else(b>a);
    {
    	if(b>c)
    	printf("b is biggest of all three",b);
    	else
    	printf("c is bigger than a and b",c);
	}
	
  return 0;

